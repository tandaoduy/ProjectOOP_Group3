@model IEnumerable<ProjectOOP_Group3.Models.DonDatHang>

@{
    ViewBag.Title = "Danh sách Đơn Đặt Hàng";
}

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold text-gray-800">Danh sách Đơn Đặt Hàng</h1>
        @Html.ActionLink("Tạo mới", "Create", null, new { @class = "bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700 transition-colors" })
    </div>

    <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mã DDH</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mã PYC</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nhà cung cấp</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ngày lập</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Ngày giao dự kiến</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tổng tiền</th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Thao tác</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                @foreach (var item in Model)
                {
                    <tr class="hover:bg-gray-50">
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">@item.MaDDH</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">@item.MaPYC</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">@(item.NhaCungCap?.TenNCC ?? "")</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">@(item.NgayLap?.ToString("dd/MM/yyyy") ?? "")</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">@(item.NgayGiaoDuKien?.ToString("dd/MM/yyyy") ?? "")</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-semibold text-gray-900">@(item.TongTien?.ToString("N0") ?? "0") đ</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                            @Html.ActionLink("Chi tiết", "Details", new { id = item.MaDDH }, new { @class = "text-indigo-600 hover:text-indigo-900" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        @if (!Model.Any())
        {
            <div class="text-center py-12 text-gray-500">
                Không có dữ liệu
            </div>
        }
    </div>
</div>

